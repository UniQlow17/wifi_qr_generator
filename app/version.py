"""Single source of truth for the app's version. Keep in sync with the
`version` field in pyproject.toml and with the git tag used for releases
(tags are "vX.Y.Z", this is "X.Y.Z")."""
__version__ = "1.0.0"
