[project]
name = "wifi-qr-generator"
version = "1.0.0"
description = "Desktop app that generates Wi-Fi QR codes"
requires-python = ">=3.9"
dependencies = [
    "customtkinter==6.0.0",
    "qrcode==8.2",
    "Pillow==11.3.0",
]

[dependency-groups]
dev = ["pytest==8.2.2"]
build = ["pyinstaller==6.10.0"]

[tool.uv]
package = false
